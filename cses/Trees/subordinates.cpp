#include <bits/stdc++.h>
using nemespace std;

TreeNode * createTree(vector<int>& vec){
    TreeNode * root = new TreeNode(1);
    int i = 2;
    for(auto& num: vec){
        if()
    }
}
int main(){

}
